{% set build = 0 %}
{% set version = '2.2.0' %}

package:
    name: magma-cuda80
    version: {{version}}

about:
    home: http://icl.cs.utk.edu/magma/
    license: BSD
    summary: 'Matrix Algebra on GPU and Multicore Architectures'

source:
    fn: magma-{{version}}.tar.gz
    url: http://icl.cs.utk.edu/projectsfiles/magma/downloads/magma-{{version}}.tar.gz
    md5: 6c1ebf4cdf63eb302ff6258ff8c49217

requirements:
    build:
        - openblas
        - libgfortran

    run:
        - openblas
        - libgfortran

build:
    number: {{build}}
    string: {{build}}
